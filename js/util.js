// Módulo de utilidades
(function() {
  // Función para comprobar si un elemento existe
  function existeElemento(elemento) {
    return elemento !== null && elemento !== undefined;
  }

  // Exportar la función existeElemento()
  export { existeElemento };
})();
